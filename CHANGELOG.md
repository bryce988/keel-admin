# 更新日志

本项目遵循[语义化版本](https://semver.org/lang/zh-CN/)，格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)。

`web/` 与 `server/` 共用同一个版本号与 tag。

## [未发布]

### 新增
- **`<ImportDialog>` 导入弹窗**，取代原先「工具栏一个导入按钮直接弹文件选择器 + 旁边一个下载模板链接」：
  - **模板下载收进弹窗**，不再长期占工具栏一个位置。原来的问题不只是占位——真正要导入的人
    点「导入」那一刻不会被提醒「得先下模板」，于是拿自己整理的表格去传，失败一堆行才回头发现
  - 上传前就写明支持格式、单次数量、大小上限，并**在前端先拦一道**：
    超大文件根本到不了后端（nginx 的 `client_max_body_size` 直接 413），
    不拦的话前端只能显示一句没头没尾的「请求失败」
  - 拖拽上传区，替代只能点击的按钮
  - **失败明细就地展示**、弹窗不关，改完表格可直接重传；原先明细弹在另一个弹窗里，
    看完一关，想重传还得再走一遍工具栏
- **岗位补全为可用功能**。此前 `sys_posts.default_role_id` 建了列、进了校验、种子里填了真实
  角色 id，文档也按「已实现」描述，但**界面上既设不了、也没有任何代码消费它**；
  更要紧的是**用户表单里根本没有岗位字段**——通过界面无法给任何人分配岗位，
  列表里能看到岗位是因为种子和 Excel 导入直接写了库。现在补齐三处：
  - 岗位表单新增「默认角色」下拉，详情页一并展示（`0` = 不带角色，是合法取值）
  - 用户表单新增「岗位」下拉，数据源是新接口 `GET /admin/posts/options`
    （只返回启用的岗位，权限 `sys:post:list` 或 `sys:user:list` 任一即可）
  - **新建**用户选中岗位时预填默认角色；换岗位时只在用户没手动改过角色的情况下跟着变；
    **编辑用户改岗位一律不动角色**——那等于「改岗位就改权限」，是这套设计刻意划的红线
- **数据导出改成异步**（`数据管理 / 数据导出`）。**BREAKING**：用户列表、操作日志、
  登录日志三处的「导出」不再直接下载文件，改为返回 `202` + `{task_id, message}`——
  点一下建一条任务投进队列，文件由消费进程生成，完成后到导出中心下载。
  自建前端或脚本调这三个接口的要跟着改：响应体不再是文件流。
  - 为什么：几万行的 xlsx 要几十秒，同步返回会在浏览器或 nginx 任一层超时，
    而文件其实已经生成好了；更要紧的是 webman 常驻内存、worker 数固定，
    一个 worker 卡在导出上，这段时间它一个请求都接不了
  - **消费进程会还原发起人身份**。这是整件事最要命的一点：数据权限在
    `Ctx::user() === null` 时**不加任何条件**，字段脱敏也读当前用户——不还原的话，
    部门主管发起的导出会生成一份全公司名单、手机号还是明文，一次点击绕过两道权限。
    实测：主管导出得到 2 行（他的部门）而不是 5 行，邮箱仍是掩码
  - 导出中心显示进度、行数、大小，跑着的任务页面每 3 秒自刷（没有未完成任务时不轮询）
  - 能不能下载由服务端算（`downloadable`），不看状态：文件会被回收而状态仍是「已完成」，
    只看状态会给出一个点了报错的按钮。文件没了下载返回 **410**（新增状态码）而不是 404——
    记录还在，用户该做的是重新导出而不是回列表
  - 文件保留 `sys.export.retainDays` 天（新参数，默认 3；原先固定 1 小时，
    异步之后那个值必然让「昨晚发起、今早来下」变成已过期）。过期记录每天 03:40 清理
  - **谁有 `xxx:export` 就必须有 `sys:export:list`**，否则他导得出来却看不到任务，
    也就永远下载不了。内置角色的授权已经一起给了
  - ⚠️ 文件写在 `runtime/exports`，消费进程生成、web 进程下载，靠同一个容器共享目录。
    **多实例部署要换成对象存储或共享卷**
- **系统公告**（`数据管理 / 系统公告`）与**顶栏消息铃铛**：
  - 公告有草稿与已发布两态，**发布 / 撤回是独立按钮与独立接口**，不是编辑表单里的一个
    状态字段——「谁在什么时候把这条发给了全员」是公告唯一真正需要审计的事，
    它在操作日志里各自成条。发布幂等：已发布的再点一次不会刷新发布时间，
    否则这条会重新顶回所有人的未读里
  - 铃铛在「界面设置」左边，有未读时角标显示条数；**你在线时新发布的公告会弹一次提示**，
    刚登录时积压的未读不弹（角标已经说清楚了，登录就被弹一屏是骚扰）
  - 点开一条即标记已读，「全部已读」一次清空。下拉里已读未读都列——
    只列未读的话读完最后一条下拉就空了，会让人以为消息丢了
  - 接收端接口（`/admin/my/notices*`）**登录即可，不挂权限点**：公告的受众是每一个
    登录用户，挂了等于「没被授权的人收不到全员通知」。管理端仍要 `sys:notice:*`
  - 没有 WebSocket：前端每 60 秒轮询一次，标签页不可见时不轮询。公告延迟一分钟没有影响，
    为它引长连接要付出的是额外进程、网关配置与断线重连
  - 修复富文本工具栏点了不生效：按钮抢走编辑区焦点，格式套在空节点上、文字落进后面
    新起的段落（点「H2」得到 `<h2></h2>` + 普通段落，点「列表」得到空 `<li>` 加两段文字）。
    工具栏容器加 `@mousedown.prevent` 即可，浏览器实测确认
  - 正文是**富文本**（新增 `<RichEditor>`，基于 tiptap；新增/编辑用**弹窗**而不是
    列表页惯例的抽屉——正文要宽行宽，560px 的抽屉会把它挤成窄条）。
    编辑器所见与详情、铃铛里所见共用同一套 `.rich-content` 样式，排版不会两样
  - **富文本在写入时按白名单净化**（服务端 `support/Html.php` + HTMLPurifier）：
    `<script>`、`on*` 事件属性、`javascript:` 协议、`<iframe>` 一律剥掉，外链自动补
    `rel="noopener"`。这段内容会出现在每个登录用户的屏幕上，是天然的存储型 XSS 放大器；
    净化放在写入侧而不是渲染侧，是因为渲染点有三处，靠每处记得做一次迟早漏一处
  - 新增两张表 `sys_notices` / `sys_notice_reads`，两个字典 `notice_type` / `notice_status`。
    **只记已读不记未读**，未读是算出来的——反过来做会让「发一条公告」变成一次全表写入
  - 新增依赖：前端 `@tiptap/*`（富文本，**只在公告页的异步 chunk 里**，首屏不受影响）、
    后端 `ezyang/htmlpurifier`（净化）。装了新的 composer 包，部署时是 `restart` 不是 `reload`
- **「数据管理」一级菜单**，数据字典从「系统管理」挪到它下面，路径随之从
  `/system/dict` 改为 `/data/dict`（**旧路径不再存在**，收藏夹或固定页签里存着旧
  地址的会落到 404，重新从菜单进入即可）。字典是业务侧每天都要维护的东西，
  和组织、权限这类搭好就基本不动的配置放在一起，找起来始终隔一层。
  概览页「字典」快捷入口的跳转地址一并更新
- **分栏布局**：第三种导航版式。一级菜单在最左 80px 窄条竖排（图标在上、名字在下），
  第二栏显示当前模块的子菜单、顶部标出模块名，面包屑与页签照常。
  与混合布局共用同一套「当前一级模块」推导，一样不重注册路由、不需要后端配合。
  这个版式下折叠按钮的语义是**收起第二栏**而不是把它压成图标条——
  左边已经有一条图标窄条了，再来一条只有图标的列分不出谁是谁；
  收起后折叠按钮仍在，否则再也展不开。一级模块少于 4~5 个时窄条会显空，
  按菜单规模自行取舍
- **混合布局**：一级菜单移到顶栏横排，左侧栏只显示当前一级模块的子菜单。
  顶栏右侧新增设置图标（头像旁），抽屉里可在「经典布局」「混合布局」间切换，选择存 localStorage。
  两种版式用的是同一棵后端菜单树，切换不重注册路由、不需要后端配合，也不改变可见内容。
  当前模块没有子菜单时（如「概览」）侧栏整列收起，把宽度让给内容区
- **顶栏全屏按钮**，在主题切换与界面设置之间。状态只由 `fullscreenchange` 事件驱动、
  不自己记布尔值——按 Esc 或 F11 退出时不经过这个按钮，自己记的话图标会停在
  「退出全屏」上，再点一下反而又进全屏。Safari 的三个前缀 API（进、出、读状态）
  各留了回退，只补其中一两个会变成「进得去出不来」。浏览器不支持时按钮不渲染，
  而不是留一个点了没反应的。表格定高会跟着重算（`ProTable` 显式听了这个事件，
  没有只靠 resize）
- **`npm run check-css`**：护栏脚本，检查全局样式里覆盖 Element Plus 的规则是否带
  `:root` 前缀，缺了就让构建失败。已并进 `npm run check`，CI 一并跑。
  这类失效不报错、不告警，DevTools 里我们那条规则也确确实实在、只是被划了删除线，
  不把两条规则并排列出来根本看不出是谁赢了——分页箭头圆角、卡片圆角、表头底色
  已经各栽一次，靠记性守不住
- **`scripts/bench-workers.sh`**：进程数逐档压测，回答「worker 加到多少就不涨了」。
  压测客户端跑在容器网络内部——在 macOS 上经宿主端口转发打，Docker Desktop 会先于应用
  饱和（实测 1874 vs 3406 RPS），各档 worker 数会压出一样的数字，看着像「加进程没用」
- **`WORKER_COUNT`** 环境变量：覆盖 HTTP worker 进程数，运维调进程数不必改代码
- **CI**：`.github/workflows/ci.yml` 三个 job —— 前端类型检查与构建、后端 `php -l` 全量与
  `composer validate`、以及起 docker compose 跑 `scripts/acceptance.sh` 的验收。
  与 CONTRIBUTING 里「提交前跑一遍检查」是同一组命令，本地过了 CI 就会过
- **`composer lint` / `composer check`**：`server/scripts/lint.php` 对 vendor 之外的全部 PHP 跑语法检查。
  CONTRIBUTING 里写了很久的 `composer lint`、`composer test` 此前并不存在，跑起来直接报错
- `docker compose exec web npm run check`：一条命令跑完 `vue-tsc` + `vite build`
  （只跑 type-check 验不到模板结构，两步必须都跑）

### 变更
- **BREAKING** **部门编码与岗位编码改由程序按主键生成，新增与编辑的表单里不再有这两项**。
  格式是 `DEPT-` / `POST-` 加四位左补零的主键：`1` → `POST-0001`，`100` → `POST-0100`，
  超过 9999 自然变五位（补零是为了按字符串排序时顺序与主键一致，不是长度上限）。
  编码的唯一用处是导入用户的 Excel 里那列「岗位编码」，让填表的人不必知道数据库主键——
  既然只是主键的可读别名，就该由程序保证一一对应；手填换来的是重复（要靠唯一索引拦）
  和同一岗位多种写法（POST-DEV / dev / 研发）。
  - `POST` / `PUT` 的 `/admin/depts`、`/admin/posts` **不再接受请求体里的 `code`**，
    带上也会被忽略（校验规则里没有这个键，`validated()` 就取不到它）
  - 新增时先入库拿到主键再回写编码，编辑不改动它——主键不变，编码就不变。
    部门挪上级时 `ancestors` 照常平移，编码不受影响
  - 列表与详情仍然显示编码，关键词搜索也照旧能搜
  - 错误码 **`20201`（部门编码已存在）与 `20801`（岗位编码已存在）已退役**：
    编码不再由人填，不存在重复这回事。号段留空不复用

  **迁移**：`migrate.php` 会把存量部门与岗位的编码统一刷成新格式（先挪进临时命名空间
  再改到位——`uk_code` 是逐行判定的，单条 UPDATE 可能撞上另一行还没腾出的旧值）。
  内置数据因此由 `DEPT-ROOT/TECH/OPS`、`POST-TECH-LEAD/DEV/OPS` 变成
  `DEPT-0001`~`0003`、`POST-0001`~`0003`。
  ⚠️ **手上存着旧编码的用户导入表格需要改「部门编码」「岗位编码」两列**，
  否则导入会报「编码不存在」；新编码在各自的管理列表里直接可见。
  `seed.php` 里岗位的对齐键也从 `code` 换成了 `name`，不换的话每次播种都会
  因为找不到旧编码而重复插一份内置岗位（部门是在 `schema.sql` 里按主键 upsert 的，不受影响）
- **BREAKING** **角色编码同样改由程序按主键生成**（`ROLE-` 加四位左补零主键，规则与部门/岗位一致）。
  但角色编码和部门/岗位编码**不是一类东西**：部门/岗位编码只是导入表格里的主键别名，
  而角色编码是**被引用的标识符**——登录响应里的 `roles` 数组、前端的 `v-role` 指令、
  `seed.php` 的授权表，都拿它匹配。所以这里有一条部门/岗位没有的迁移提醒：
  - `POST` / `PUT` 的 `/admin/roles` **不再接受请求体里的 `code`**，列表与详情仍显示
  - `migrate.php` 把存量角色编码统一刷成 `ROLE-0001`~`ROLE-000N`（内置的
    `ROLE_SUPER`/`ROLE_DEPT_MGR`/`ROLE_STAFF` → `ROLE-0001`/`0002`/`0003`）；
    `seed.php` 里角色授权与演示用户的**对齐键从编码换成名称**，否则每次播种都会
    因为找不到旧编码而重复建角色、或者静默少授权
  - ⚠️ **业务代码不要再硬编码 `ROLE-0007` 这类值**——角色编码依赖插入顺序，
    不同环境（尤其正式库与重建的测试库）同一角色的编码可能不同，`v-role="'ROLE-0007'"`
    会静默失效。要按「用途」匹配角色，用一套「用途 → 角色名称」的配置，
    运行时按名字查 id / code 再喂给 `v-role`（指令注释里已加同样提醒）
- **用户状态由三档（在职 / 试用期 / 停用）归并为两档「启用 / 停用」，列表里改用开关直接切换**。
  「试用期」是人事状态不是权限状态——`AuthService` 从头到尾只判 `status === 0`，
  它和「在职」在登录与鉴权上完全一样，唯一的作用是逼着每处判断写 `!== 0` 而不是 `=== 1`。
  脚手架不带业务语义，这一档删掉；真需要人事状态的项目该另立字段，别挤在权限开关里。
  - 列表状态列从标签换成 `el-switch`，**点击先弹二次确认再提交**，不是先翻再问「要撤销吗」；
    「更多」里原来的「启用 / 停用」一项随之取消，同一件事不再有两个入口
  - 超管与没有 `sys:user:update` 的人看到的是灰掉的开关：**读得出状态，按不动**
  - 提交成功只改那一行的字段，不整表重拉——翻一个开关就重置分页、滚动位置和多选，
    代价远大于同步一个字段
  - **存量库自动对齐**：`migrate.php` 新增「数据补丁」一节，把 `status = 2` 的行并到 1
    并更新列注释；`seed.php` 新增「退役字典项」清单，删掉字典里的「试用期」——
    播种只 upsert 从来不删，不点名清理的话它会一直留在筛选下拉里，选中后永远筛不出东西
  - **接口收紧**：`PUT /admin/users/{id}/status` 与列表、增改的 `status` 一律只收 `0` / `1`，
    传 `2` 返回 422
- **列表首列由序号改为主键 ID**，居中、固定在左侧（`ProTable` 的 `index` prop 改名为 `id-column`，
  九个列表页一并更新）。序号是「第几行」，翻一页就变，指认不了任何东西——
  排查问题时说「第 3 行」没用，说「ID 12」才对得上库里的记录、操作日志里的对象和接口返回的主键。
  多选列一并固定：只固定其中一个的话固定列会被拉到非固定列左边，表头会变成「ID | ☑ | 名称」。
  树形表格（部门、菜单）不出这一列——EP 把展开箭头画在第一个普通列上，加了 ID 树的层级就散了
- 用户页的「导出」补上 loading 态，此前点完没有任何反馈
- **圆角分两档**：容器（面板、卡片、对话框、登录卡）由 4px 提到 **8px**，控件（按钮、页签、
  图标按钮、分页、输入框）保持 4px。此前全站一个 4px，大卡片的边角几乎是直的、偏工程感；
  但控件也提到 8px 会圆过头——30px 高的页签会接近胶囊。新增 `--keel-radius-lg` 令牌
- **阴影底色由纯黑改为 `#303133`**（EP 中性色里最深的一档，`--el-text-color-primary`
  的值）：纯黑阴影会把底下的颜色往灰里拉，白底卡片周围会出现一圈发脏的灰；
  抬一点点明度，边缘看着是暗而不是脏。取 EP 自己的色而不是随便找个深色，
  是为了让全局样式里不出现任何 EP 之外的颜色——阴影也是颜色。
  顺带修掉 EP `--el-box-shadow-light` 没有垂直偏移的问题（`0 0 12px` 是一圈光晕不是阴影，
  登录卡用的就是它）。**深色模式单独一套纯黑高不透明度值**——`#303133` 投在 `#0a0a0a`
  的页面底色上等于隐形，深色界面的浮起靠的是把浮层周围压暗
- **搜索栏改为等宽栅格，字段按列对齐**：此前 label 宽度随字数走（「模块」2 字 vs
  「TraceID」7 字符）、控件宽度按类型分四档（输入框 220 / 下拉 160 / 日期 150 / 区间 260），
  于是多行时没有任何两列对得齐。现在 label 固定 72px、控件撑满等宽格子，
  各行的 label 起点、控件左右边缘三条线都对齐；窄屏按 4→3→2→1 列自动降
- **搜索栏的「查询 / 重置」改为独占右侧一列**：此前它和字段在同一个换行流里，紧跟最后一个字段
  ——操作日志点一下「展开」，按钮从第一行末尾掉到第二行、横向移动近 300px，眼睛得重新找；
  字段数不同的列表页之间也各在各的位置。现在字段单独包一层、只在自己区域内折行，
  按钮所在的竖带始终不被字段占用，展开/收起与各页面都是同一条垂直线
- **HTTP worker 进程数默认值由 `核数 × 4` 改为 `max(8, 核数 × 2)`**：逐档压测显示吞吐拐点
  在 ×2，×4 内存翻倍而吞吐持平（10 核机：×2 为 4119 RPS / 553MB，×4 为 4081 RPS / 1086MB），
  ×8 开始倒退。2 核的生产机仍是 8 个 worker，**行为不变**；核多的机器不再白占内存。
  保底 8 个是给导出这类长阻塞接口留的余量
- **生产改为不可变镜像**（`docker/php/Dockerfile.prod`）：后端代码与 composer 依赖在**构建期**
  烤进镜像，容器以只读根文件系统运行。此前生产把整个 `server/` 挂载进容器、并在每次启动时
  执行 composer 安装，等于「线上跑的是 git 工作副本的当前状态」——不是任何一个可复现的产物，
  也无法回滚到上一个镜像
- **建表与播种改为一次性 job**：`docker-compose.prod.yml` 新增 `migrate` 服务
  （migrate → install → seed），`server` 声明 `service_completed_successfully` 依赖它。
  初始化失败会直接拦住发布，而不是像原来那样被 `|| echo` 吞掉、留下一个权限树残缺的线上环境
  （表现是登录进去到处 403）
- **生产不再启用文件监听**：`config/process.php` 的 `enable_file_monitor` 增加 `APP_ENV` 判断。
  上游只按 `-d`（守护进程）判断，而容器里不能用 `-d`，于是生产一直开着文件监听——
  每两秒扫一遍 `app/`，且**任何文件改动都会自动 reload 线上进程**
- **前端 Element Plus 改为按需导入**（`unplugin-vue-components` + `unplugin-auto-import`）：
  主 JS 从 1275 KB / gzip 414 KB 降到 816 KB / gzip 269 KB（-36%），
  主 CSS 从 367 KB / gzip 50 KB 降到 164 KB / gzip 23 KB（-54%）
- **`ProTable` 与表单壳改为泛型**：`ProTable<T>`、`useFormShell<T>`、`FormDrawer<T>` / `FormDialog<T>`，
  行类型与表单类型从 `api/*.ts` 一路推到模板。全仓 28 处 `Record<string, any>` 清零。
  此前表格列、表单字段与接口字段对不上要等用户点开那一页才发现
- 写接口的请求体类型收紧：`api/system.ts` 的 16 个 create/update 由 `Record<string, unknown>`
  改为 `UserPayload` / `DeptPayload` 等（`Partial<行类型>`），字段名拼错在编译期就报错
- `Cache` 不再在每次访问前 `PING`，改为失败后重连并重试一次。原写法给每一次缓存读写
  固定加一个往返，而断连是罕见事件；Redis 若部署在网络对端（托管实例），这笔开销会显著放大
- 前端生产镜像改用 `npm ci` 并拷入 `package-lock.json`：此前只拷 `package.json` 跑 `npm install`，
  每次构建都重新解析版本范围，线上装到的依赖树可能与锁文件不是同一套
- 菜单的 `visible` / `keep_alive` 前后端统一用布尔：接口返回的本就是布尔，
  而表单打开时转成 1/0、提交时又送回数字，靠 PHP 的隐式转换兜住
- **全站字体改为系统 UI 字体栈**（`-apple-system` / `Segoe UI` / `Roboto` …，
  中文依次回退 苹方 / 冬青黑 / 微软雅黑 / 思源黑）：各平台显示自己的原生界面字体，
  不再统一落到 Helvetica Neue。覆盖的是 `--el-font-family` 令牌而非只给 `body` 设
  `font-family`，Element Plus 组件内部（表格、下拉、消息框）也一起跟着变
- **全站配色改用 Element Plus 原生调色板**，不再自定义品牌色。此前 `styles/index.css`
  写死了一套饱和度更高的（primary `#3986FF`、success/warning/danger 取 Tailwind 的
  green-600 / orange-500 / red-600），浅色深色各七档共 70 个色值。现在全部删掉，
  由 EP 的 `el-var.css` 与 `dark/css-vars.css` 供给：

  | 角色 | 现在（EP 原色） | 此前 |
  |---|---|---|
  | primary | `#409EFF` | `#3986FF` |
  | success | `#67C23A` | `#16A34A` |
  | warning | `#E6A23C` | `#F97316` |
  | danger | `#F56C6C` | `#DC2626` |
  | info | `#909399` | `#5D5B6E` |

  中性色（文字、边框、填充、页面底色）本来就是 EP 原生的，未动。

  品牌色**零覆盖**：标签、链接按钮、实心按钮全部走 EP 默认。
  中途试过给「纯文字」形态降一档用 `dark-2`（EP 自己的深一档色），
  为的是把压在浅底上的文字对比度从 2.0~2.8 提上去，两次都撤了——
  同一屏里出现两种同名色（链接 `#337ECC` 挨着实心主按钮 `#409EFF`），
  观感上像是链接被禁用，比对比度低更糟。结论记在了 `styles/index.css` 里：
  在「只用 EP 调色板」的前提下对比度没有免费的解，真要提就得换主色重编 EP 主题，
  而不是在个别组件上打补丁
- **字重收敛成两档**，这才是「字体看着不协调」的实际来源。此前同一屏的 14px 上
  同时有四种字重：400 正文（59 处）、500 按钮与链接（18）、600 面板标题与表头（10）、
  700 面包屑分隔符与分页当前页（2，是 EP 默认值，没人注意到——那个「/」是文本不是
  图标，EP 给了 `font-weight:700`，于是分隔符比它两边的文字都重）。
  字号一样、字重四档，等于让字重同时干四件事。现在的规则写进了 `styles/index.css`：
  **14px 及以下只有 400 / 500，16px 及以上才允许 600**（字号已经跨了一档，
  字重再跨一档才不会打架）。表头、面板标题从 600 降到 500，EP 那两处 700 拉回。
- 品牌字号由 `size * 0.64` 改为定死 **20px**。原来 size 默认 28，算出来是 **17.92px**
  ——全站唯一一个小数字号，浏览器按亚像素栅格化，同一字重在 17.92 与 18 下的笔画
  粗细肉眼可辨。logo 是版式里最大的字，本来就该单独定值，不该由图标尺寸推导
- 仪表盘统计数字 26px → 24px，回到字号阶梯上
- **列表正文一律居中，左对齐只留两种例外**。此前对齐方式是逐页随手写的：
  同为登录日志的「时间」在登录日志页居中、在个人中心页却左对齐；操作日志的「耗时」
  是全表唯一一个右对齐的列，看着像漏改。而表头本来就是居中的，正文靠左时两者
  错开半个格子，一屏扫下来是歪的。九个列表页的正文对齐已统一，例外只有：
  - **树形表格的名称列**（部门、菜单）——展开箭头与层级缩进画在这一列上，
    居中会把缩进吃掉，父子层级就看不出来了。这是硬约束，不是审美
  - **会被 tooltip 截断的自由文本**——备注、登录地址、操作对象、接口路径、
    组件路径、失败说明。这类列每行长度差好几倍，居中首尾都参差，比靠左更难扫

  约定与这两条例外写进了 `ProTable` 的列定义注释
- **表格操作列的按钮统一加图标**：详情 `View`、编辑 `EditPen`、删除 `Delete`、
  新增下级 `Plus`、授权 `Key`、成员 `User`。此前只有角色页的「授权」「成员」两个有图标，
  其余七页全是纯文字，同一套操作在不同页面长得不一样。
  「更多」的箭头放在文字**右边**（`el-icon--right`）而不是左边：左置图标说的是
  「这个动作是什么」，向下箭头说的是「点了会展开一层」，是方向提示，跟着文字走才读得通。
  用户页「更多」下拉里的三项也一并补齐（重置密码 `Key`、启用/停用 `Open`/`TurnOff`、
  删除 `Delete`），图标跟着**将要执行的动作**走而不是当前状态。

  图标与文字的间距从 EP 默认的 6px 收到 **2px**：那 6px 是按 14px 正文、独立按钮定的，
  而这一列字号已降到 13px，两个汉字才 26px 宽，6px 的间隙占到标签宽度四分之一，
  图标看着像浮在旁边没跟文字成一体。实测图标盒与内部 svg 等宽（13px），
  但字形在 viewBox 里自带留白、笔画不顶边，所以 2px 落到眼睛里仍有呼吸感。
  「更多」右侧那个箭头不动（EP 的 5px）——它是方向提示不是动作图标，
  离得开一点反而更像「后面还有」
- **操作列的可见槽位固定为三个**，超出的收进「更多」。加图标后角色页五个动作要 287px，
  比这张表任何一个数据列都宽，操作入口反倒成了整屏最重的东西。

  三个槽位的分配是固定的：**详情**永远在第一个（它是唯一不需要权限的动作，
  任何能看到这张表的人都点得到，位置固定下来眼睛就不用每页重新找）、
  中间放这一页最常用的那个、第三个是「更多」。

  | 页面 | 可见 | 「更多」里 |
  |---|---|---|
  | 角色 | 详情 · **授权** | 成员 · 编辑 · 删除 |
  | 部门 / 菜单 | 详情 · **编辑** | 新增下级 · 删除 |
  | 用户 | 详情 · **编辑** | 重置密码 · 启用/停用 · 删除（原本如此） |

  角色页中间放「授权」而不是「编辑」：改名排序偶尔为之，配权限是天天要做的。
  菜单页的「新增下级」在下拉里仍然判 `type <= 2`——按钮/接口/数据是叶子，
  不判的话叶子节点的下拉里会出现一个点了报错的项。

  于是十个列表页的操作列宽收敛成三个值：**三个动作 210 · 两个 150 · 一个 120**
  （角色 250→210、部门 230→210、菜单 210→210、字典 180→150、操作日志 90→120）。
  逐页在浏览器里量的，八个页面溢出全为 false
- **表格操作列的链接压一档**：13px / 400，而不是继承 EP 按钮的 14px / 500。
  这一列每行有三到五个链接，二十行就是六十到一百个蓝色词，用正文同号再加一档字重，
  整张表最抢眼的会是「详情 编辑 删除」这些**入口**而不是它们指向的数据，恰好反了。
  字重先降——链接本来就有颜色，颜色已经是「这里能点」的信号，字重是重复表达。

  同时**把点击区从 20px 提到 24px**（`min-height`）。EP 给 `is-link` 的内边距只有 2px，
  原本就低于 WCAG 2.5.8 建议的 24px 最小目标尺寸，再降字号只会更小。
  实测这一条对行高的代价是 **0px**（EP 的 `.cell` 行高本来就有 23px 的底），
  所以「看着更轻」和「更好点」不必二选一
- **列宽一律用 `minWidth` 而不是 `width`**：`width` 是死值，宽屏上表格右边会空一块；
  `minWidth` 是下限，有富余时会把剩余空间摊进来。只有图标这类真不该变宽的才用 `width`

- **登录页改为左右布局**：左品牌栏（标识 + 标语 + 页脚），右表单栏（标题 + 三个输入 + 按钮），
  同一张卡片内分栏。此前是一张 400px 的窄卡竖着堆，页脚孤零零飘在卡片外面。
  - 品牌栏的背景是一组**同心的船体肋骨弧线**，取自 `BrandLogo` 的同一条曲线，
    用 SVG 现画。不用插画：外链过不了 CSP 与离线部署，内联又是一大坨 base64；
    而且这个脚手架是给人 fork 的，插画一旦有具体形象，改名换色之后就格格不入，
    一组线是结构性的，`currentColor` 跟着主色走
  - 窄屏（< 860px）左栏收成一条横带，只留标识那一行，标语、装饰、页脚三样收掉——
    品牌栏原样堆在表单上方会把输入框推出首屏，而登录页唯一要紧的就是那三个输入框。
    实测 430px 视口下登录按钮仍在首屏内
  - 深浅色都验过：品牌栏底用 `--el-color-primary-light-9`，浅色是浅蓝、深色是深蓝，
    一条规则通吃，没有额外的深色分支
  - 三个输入框加了前置图标（账号 `User`、密码 `Lock`、验证码 `Picture`）。
    这个表单只有占位符没有 label，图标是仅有的常驻标识。
    验证码没用常见的 `Key`：它和上一行的 `Lock` 是一对近义图形，挨着放会让人
    分不清哪行才是密码

### 性能
- **前端首屏 JS 从 816 KB 降到 404 KB（gzip 269 → 135 KB）**：通用组件此前既被
  `main.ts` 全局注册、又被 unplugin-vue-components 按页注入，等于注册了两遍。
  而全局注册那条路径把 `ProTable` / `SearchForm` 钉进了首屏依赖图，连带
  `el-table`、`el-date-picker`、`el-time-picker` 一起——**只有一个输入框的登录页**
  也得先下完这套表格和日期选择器。去掉多余的全局注册后，它们跟着懒加载的页面走
- **首屏拆成三个 chunk**，业务代码与第三方依赖分开缓存：业务 72 KB（gzip 29 KB）/
  `vendor` 161 KB（vue、vue-router、pinia、axios）/ `icons` 171 KB。
  改一行业务代码，用户重新下载的从 269 KB 变成 29 KB。
  构建的「chunk 超过 500 KB」告警一并消失
- **概览页 SQL 从 23 条降到 14 条**：`stats()` 与 `moduleSummary()` 此前各查一遍用户、部门、
  岗位、角色；「总数」与「其中多少条满足某条件」也拆成了两条 `count()`。
  后者不只是多一次往返——两次查询之间有人登录，就会出现「今日登录 7 次、失败 8 次」这种对不上的数
- **字典列表的 N+1 收敛**：字典类型列表 14 → 5 条 SQL；字典项列表的引用数改为整页批量聚合，
  查询条数与页大小无关（`page_size=100` 时从约 400 条降到 8 条）
- **日志表补索引**：`sys_operation_logs` 增加 `(dept_id, created_at)`；
  `sys_login_logs` 增加 `(created_at)` 与 `(dept_id, created_at)`，并删掉被前缀覆盖的单列 `idx_dept`。
  登录日志此前按时间范围查询时 `possible_keys` 为 `NULL`（一个候选索引都没有）

### 修复
- **两个下拉在「0」这个取值上显示成一个光秃秃的 `0`**：新增岗位时的「默认角色」、
  新增用户时的「岗位」。两处的 0 都是**取值**（不带角色 / 未设置）而不是「没选」，
  原来靠 `clearable` + `placeholder` 表达——但 placeholder 只在值为空时才出现，
  而初始值就是 0，`el-select` 找不到 id 为 0 的选项就把原始值直接渲染出来。
  改成在候选里显式放一项 id = 0。全站其余带「零值」的选择器
  （用户的部门 = 未分配、角色的继承自 = 不继承、部门与菜单的上级 = 顶级）
  本来就是这么做的，这两处是漏网的
- **开关的行内状态文字在关态几乎看不见**。EP 把 `inline-prompt` 的文字定死为白色，
  而关态的轨道是 `--el-border-color`（浅色主题 #dcdfe6）——白字压浅灰实测 **1.33:1**，
  不是「偏淡」是根本读不出那两个字。关态改用正文色（9.8:1），
  深色模式下轨道与正文色一起翻面，仍是深底浅字（7.0:1），不用另写一套。
  开态的白字压主色不动：那与全站主色按钮是同一套观感，单独改这一处反而不一致
- **播种脚本改完字典标签，界面最长五分钟后才变**。`seed.php` 直接写库，
  而 `DictService` 把每类字典缓存在 Redis 里（TTL 取 `sys.cache.ttl`，默认 300 秒），
  两边没有连接。表现是「部署完了但界面没变」，最容易被当成改动没生效。
  播种末尾统一清一次字典缓存；Redis 连不上只提示不中断——缓存过期本来就会自愈
- **表单里说明文字的位置不一致**。`el-form-item` 的内容区是 flex 容器，
  控件占满整行时说明自然换行，而状态那项换成 48px 宽的开关之后，
  右边剩的空间足够把说明拽到同一行去。说明统一占满一行
- **鼠标移到操作列的「更多」上，两侧各出现一条淡蓝色竖线**，上下没有横边，看着像渲染坏了。
  是两件事撞在一起：
  1. `el-dropdown` 展开时会**主动 focus 触发元素**，Chrome 于是画上 `:focus-visible`
     焦点环——EP 的规则是 `outline: 2px solid; outline-offset: 1px`，
     颜色 `--el-color-primary-light-5`（#a0cfff），与看到的淡蓝一致
  2. 这个环比按钮盒大 3px（1px 偏移 + 2px 线宽），而 EP 的 `.el-table .cell`
     带 `overflow: hidden`、高度就等于内容的 24px——环的上下两条边落在盒外被裁掉，
     只剩左右两根竖线

  偏移改成 `-2px`，环画在按钮内侧，整圈都在可见区域内。没有用 `outline: none`
  一关了事——那是键盘用户唯一的位置指示。另外单独压掉了**指针悬停时**的环：
  纯鼠标动作不该触发纯键盘的视觉反馈，而 Tab 过来时鼠标通常不在这儿，环照常出现
- **部门下拉选中后输入框里带缩进空格**：用户表单与岗位表单的部门下拉用全角空格拼进 label 做
  层级缩进，于是选中二、三级部门后，输入框里的名字前面也跟着一串空格（选一级看着正常，
  选下级就歪）。改成缩进只渲染在下拉项上（层级 × 16px 的左内边距），label 保持纯部门名
- **「参数配置」里改系统名称、Logo、页脚不生效**：后端的免登录接口
  `/admin/params/public` 和白名单（`ParamService::PUBLIC_KEYS`）一直都在，
  **前端从没调过一次**——侧栏品牌名写死在 `BrandLogo` 的 prop 默认值里，
  登录页页脚写死一行字面量。
  现在启动时拉一次存进 app store，供侧栏、登录页、浏览器标签标题三处使用：
  - 不 `await`：站点名不值得让整个应用等一个网络往返，后端不可用时更会卡成白屏。
    store 里有兜底值，参数到达后补写一次标题
  - 配了 `sys.logo` 就用那张图，**加载失败或没有固有尺寸时退回内置矢量标记**。
    后者是实测撞出来的：只有 `viewBox`、没有 `width`/`height` 的 SVG
    会「加载成功但画不出来」，浏览器显示碎图（本仓库自己的 `favicon.svg` 就是这种）
  - `sys.footer` 的种子默认值改为 `Keel v1.0.0 · MIT License`，与此前写死的那行一致，
    默认部署的观感不变（种子只对全新安装生效，不覆盖已改过的值）
- **表头没有底色，与表体一样白**，一排表头看不出是表头。这是按需导入引入的
  同一类静默失效，第三次了：EP 把 `--el-table-header-bg-color`（默认值是白）
  声明在 **`.el-table` 这个和我们一模一样的选择器**上，特异性相同，只能比谁在后面；
  而按需导入之后 EP 的表格样式在 `el-table-column-*.css` 这个懒加载 chunk 里，
  进列表页时才插进 `<head>`，永远排在入口 `index.css` 之后——于是它赢。
  已给全部 EP 覆盖规则加 `:root` 前缀（特异性 0,2,0 对 0,1,0），不再赌加载顺序。
  底色同时从 `--el-fill-color-lighter`（#fafafa，与白底只差 2%，生效了也像没底色）
  换成 `--el-fill-color-light`（浅色 #f5f7fa / 深色 #262727，两种模式共用一条规则）
- **多个列表字段被截断成 `2026-08-28 09:4…`**，且因为 `show-overflow-tooltip` 默认开着、
  鼠标移上去有完整值，一直没被当成 bug。实测（macOS 系统字体 14px，
  加上单元格左右各 16px 内边距）：

  | 列 | 内容 | 需要 | 原先写的 |
  |---|---|---|---|
  | 最后登录 / 时间 | `2026-08-28 09:47:23` | 176 | 160 / 165 |
  | 姓名 | `系统管理员` | 101 | 100 |
  | 登录地址 | `中国广东省深圳市【电信】` | 189 | 120（个人中心） |
  | 权限标识 | `sys:role:member:remove` | 191 | 190 |
  | 角色编码 | `ROLE_DEPT_MANAGER` | 186 | 160 |
  | 模块 | `系统管理/菜单权限` | 151 | 130 |
  | IP | `255.255.255.255` | 143 | 130（操作日志） |
  | 数据范围 | `本部门及下属`（标签） | 135 | 130 |

  已按实测值统一：日期时间 190、归属地 200、权限标识 210、角色编码 200、
  IP 150、手机号 140、模块 160、姓名 120、账号与操作人 130。
  `ProTable` 的列定义处补了这张表和重新测量的方法（一行 `swift -e`，不用开浏览器）。

  ⚠️ 这一条修了两轮：第一轮按 EP 文档的 `padding: 0 12px` 算，每列少留 8px——
  `styles/index.css` 早就把单元格留白改成了 `0 16px`。差 8px 不会整列崩，
  正好卡在「大部分值能显示、偏长的那个截断」，所以第一轮改完
  「姓名」列的 `系统管理员` 仍然是 `系统管…`。上表与 `ProTable` 里的宽度表
  都已按 **+32px** 重算
- **分页左右箭头的圆角一直是 EP 的 2px**，与页码的 4px 对不上。根因是按需导入改造
  （`174f2f5`）之后 EP 的组件样式变成**按页注入**、排在 `styles/index.css` 之后，
  同特异性下后者胜出，我们那条 `.el-pagination button` 覆盖从此静默失效。
  已给受影响的选择器加 `:root` 前缀提高特异性。这类失效不报错、DevTools 里变量值也"对"，
  只有把两条规则并排列出来才看得见是谁赢了
- **不建 `.env` 直接 `docker compose up -d` 时登录会 500**：`JwtService` 的密钥长度
  下限写死为 16 字节，那是 `firebase/php-jwt` ^6 时代的判断；^7 自己会校验 HMAC
  密钥长度，HS256 要求至少 32 字节。开发编排的默认值 `change-me-in-production`
  只有 23 字节，正好落在两道检查中间，于是穿过我们的校验、栽在库里，
  抛出指向第三方库行号的 `Provided key is too short @ JWT.php:701`。
  现在下限按算法推导，报错也改成能照着做的话；开发默认值加长到 44 字节，
  README 说的「clone 完 up -d 就能跑」这才成立
- **验收脚本的期望值不再写死**：`scripts/acceptance.sh` 第 4、5 节把「总计 5 人」
  「营销部(7)」「角色 4 是审计员」当成既定事实，而那是作者本机累积出来的状态——
  种子数据里只有 4 个用户、3 个部门、3 个内置角色。本机能过是因为库里恰好有那些数据，
  CI 每次都是空库，必挂。现在人数与部门 id 一律从库里现算，
  「自定义部门范围」所需的角色就地创建、跑完删除；`down -v` 后连跑两遍均 43/43
- **`scripts/migrate.php` 现在能补索引**：此前只能补列，往 `schema.sql` 的 `CREATE TABLE` 里
  新加的 `KEY` 对存量库完全无效——开发机 `down -v` 重建后一切正常，线上一个索引都没加。
  索引缺失不报错、不返回错数据，只是慢，等发现时已经是线上慢查询
- **新增 `.dockerignore`**：此前构建上下文没有过滤，`COPY server/ ./` 会把宿主机的 `vendor/`
  覆盖到构建期装好的那份上（可复现性归零），并把运行期日志、历史导出文件、陈旧的 `webman.pid`
  一起打进镜像；宿主机存在 `server/.env` 时还会把生产密钥烤进镜像层
- **生产的用户上传目录改为挂卷**：`public/uploads` 落到 `server-uploads` 卷。
  改成不可变镜像后若不挂卷，每次发版重建镜像会把用户头像全部清掉
- `ProTable` 的 `sort-change` 签名由 `prop: string` 改为 `prop: string | null`：
  el-table 在「升序 → 降序 → 取消」的第三下会把它清空
- 字典项的 `tag_type` 由 `string` 收紧为 Element Plus 的 tag type 联合，
  后端存进拼错的值时不再只是静默显示成默认灰

### 安全
- **登录锁定阈值改由「参数配置」控制**。`sys.login.failLimit` / `sys.login.lockMinutes`
  这两项此前**没有任何代码读过**：锁定功能本身是齐全的，但阈值取自 `.env` 的
  `LOGIN_FAIL_LIMIT` / `LOGIN_LOCK_MINUTES`。所以在界面上把失败次数改成 3，
  保存成功、没有报错、列表显示新值，实际锁定行为纹丝不动——这比「功能没做」更糟，
  没做至少看得出来，而这是**看着生效其实没生效**的安全设置。
  现在的取值顺序是 **参数表 → `.env` → 字面量**，并夹到合法区间：
  阈值以前只有会改 `.env` 的人能动，现在任何有 `sys:param:update` 的人都能填，
  而 `failLimit = 0` 会让第一次输错密码就锁号、`lockMinutes = 0` 会让计数器 TTL
  变成 0 直接废掉锁定机制。`<= 0` 一律回落 `.env` 而不是夹到下界——
  这两个阈值都没有「0」的语义，0 只可能是留空或填错，
  夹到下界（错 1 次就锁）是其中最糟的解释。
  IP 总闸（`LOGIN_IP_FAIL_LIMIT`）仍只读 `.env`：种子里没有对应参数，
  不凭空造一个界面上看不见的键

### 文档
- `CONTRIBUTING.md` 的检查命令改成实际能跑的：此前四条命令有三条不存在
  （`composer lint`、`composer test` 未定义，`pnpm lint` 既无脚本、包管理器也早已换成 npm）；
  本地开发一节的 PHP 版本下限由 8.1 更正为 8.4，pnpm 改为 npm
- `PROJECT.md` 的「没有 CI」一节改为 CI 说明，并明确列出 CI 里**仍然没有**的东西
  （单元测试、ESLint / Prettier、依赖漏洞扫描）与暂不引入的理由

### 计划中
- 单元测试、ESLint / Prettier、依赖漏洞扫描
- 图标包（293 个，现已单独切成 `icons` chunk，171 KB / gzip 44 KB）仍在首屏：
  布局外壳静态引用了其中十来个，只要有一处静态 import，整包就会被钉在首屏，
  动态 import 会被 Rollup 并回去。真要拆出去得把那些图标抄成本地组件，
  但侧边栏图标名由后端下发、可以是 293 个里的任意一个，改异步会让图标先兜底再跳变。
  这是 DB 驱动图标名的固有代价，要动得先改设计（如收敛成白名单），不是改打包配置
- 进程数保底 8 个目前是推理值：防的是导出这类长阻塞接口占满小池子，
  演示库数据量太小压不出来，接真实业务后应复测

## [1.0.0] - 2026-08-24

首个公开版本，v1.0 规划的全部内容已完成：权限与数据权限、操作日志、四端隔离、
页型模板与通用组件、队列与定时任务，均已实测通过并部署上线。
侧边栏里没有「点进去是占位页」的条目。

### 新增

**项目基建（M1 之前）**
- 交互原型定稿：16 个页面，含五种页型模板与系统管理全套
- 项目文档 `PROJECT.md`：技术选型、多端架构、RBAC 设计、接口约定、部署规范
- 数据库设计 `docs/database.md`、接口契约 `docs/api.md`（通用响应约定、错误码表、各模块接口清单）
- 开源仓库文件：README、CONTRIBUTING、SECURITY、LICENSE、CHANGELOG
- Docker 一键开发环境：mysql 8 + redis 7 + webman + vite，宿主机只需 Docker
- 后端基础设施：webman 多应用目录、`TraceMiddleware`（traceId 与上下文清理）、
  统一异常处理器（HTTP 状态码 + 业务码两层）、`Db` / `Cache` / `Ctx` / `Result`
- 登录闭环：SVG 图形验证码（不依赖 GD）、JWT 签发与校验、登录失败锁定、登录日志、
  权限版本号机制、鉴权中间件、用户与菜单下发
- 前端骨架：登录页、布局、路由守卫、请求封装（先按 HTTP 状态码分派，再按业务码细化）
- 生产编排 `docker-compose.prod.yml`：前端静态化 + nginx 托管，MySQL / Redis 不暴露宿主机端口，
  构建源可配置（`APK_MIRROR` / `COMPOSER_MIRROR`）；部署脚本支持从 Git 拉取更新
- 部署上线：http://43.143.249.52:8080

**M1 框架骨架**
- 后端 14 张表 + Eloquent 模型层（`Sys*Model`），表结构由 `scripts/migrate.php` 在进程启动时幂等对齐，
  权限点 / 字典 / 参数由 `scripts/seed.php` 按唯一键 upsert
- 数据权限：`HasDataScope` trait 以模型全局 Scope 注入五种范围，业务代码不手写归属过滤，
  必须绕过时用 `Model::withoutDataScope()` 让绕过点在 review 中可见
- 权限中间件 fail-closed：接口的权限点在 `config/route.php` 上声明，不声明即 403；
  声明 `log` 的路由自动进操作日志（含越权尝试），service 内用 `OpLog::target()` / `OpLog::diff()` 补对象与字段变更
- 四端隔离：`app/{admin,client,open,internal}` 各有中间件与异常处理器，错误体结构刻意不同；
  员工 token 与 C 端 token 互调一律 401
- 日志三通道：`app`（业务）/ `error`（未捕获异常，留 30 天）/ `sql`（慢查询，阈值 `SLOW_QUERY_MS`）
- 前端：`v-permission` 指令、菜单驱动的动态路由、`ProTable` / `SearchForm` / `DictSelect` / `DictTag`、字典 store

**M2 系统管理**
- 部门与岗位、菜单与权限、角色、用户（含 openspout 导入导出）、数据字典与参数配置、操作日志与登录日志
- 写接口通用件：`Guard` / `BatchResult`；表单统一走抽屉，`ProTable` 支持树形
- 列表页筛选条件与页码同步到 URL，页签记住完整地址
- 登录日志新增登录地址（ip2region，`vectorIndex` 用 `file` 策略）

**M3 页型模板与个人中心**
- 个人中心 `/profile`：`ProfileService` 的 id 只从令牌取，结构上没有「改别人」的路径，因此不需要权限点；
  支持换头像。换绑手机用当前密码验证而非短信（脚手架不绑死短信服务商）
- `views/template/` 五种页型模板 + `_demo.ts` 内存假数据 + README，注册为开发环境专属路由
- `<EmptyState>`（四场景，必带动作）与 `<PageSkeleton>`（list / detail / form），
  收敛全仓 10 处各写各的 `el-empty`；`ProTable` 区分「没数据」与「筛不到」
- 队列消费进程（`webman/redis-queue`，count=2）+ 定时任务进程（`TaskProcess`，count=1，只投递不干活）+
  `LogCleanupService` 按 `sys.log.retainDays` 分批删

**M4 联调加固**
- `scripts/acceptance.sh`：权限矩阵与异常场景 43 项断言，含 client / open 空壳链路
  （渠道头强制、各端错误体结构不同、员工 token 调 C 端 401）
- MySQL 断开重连实测通过，无需改代码，结论写进 `Db.php` 注释
- 30 分钟压测：284,920 请求 / 1 次失败，worker 预热到 26MB 后进入平台期；
  同时量出 reload=0 秒、restart≈2 秒的停机窗口

**其他**
- 顶栏菜单命令面板、图标选择器网格面板、页签取消数量上限并把操作收进统一菜单
- 主题切换改用 View Transitions
- 品牌标记：侧栏与登录页不再是纯文字

### 变更
- **BREAKING** 模型类名统一 `Sys*Model` 后缀；接口字段一律 `snake_case`（请求与响应），
  与数据库字段名逐字一致，全链路不做键名转换。
  迁移：调用方需同步改键名，例外只有 HTTP 头与前端自己的标识符
- 校验器换成 `webman/validation`，规则收敛到 `FormRequest`
- 业务码与 HTTP 状态码常量化，前后端各一份并加一致性校验
- 表单壳拆成 `FormDrawer` 与 `FormDialog`，公共逻辑收进 `useFormShell`
- 能用模型的一律走模型，不再手写 `Db::table`；表的固定字段常量化，开关型 `status` 抽成 `HasStatus`
- 系统概览改成一级菜单并按现有模块重构；seed 补上退役菜单清单
- 全局组件尺寸、表格密度、版式令牌多轮对齐；换掉 Element Plus 默认的五个品牌色
- 升级到 PHP 8.4（随 openspout 引入）
- `tsconfig.json` 去掉已弃用的 `baseUrl`，`paths` 直接按 tsconfig 目录解析
- GitHub 与 Gitee 同为主仓库，移除模板与镜像同步工作流

### 移除
- 角色 × 权限矩阵页（权限在角色编辑内完成，矩阵页是重复入口）
- 无引用的 `ModulePlaceholder`

### 修复
- nginx：`/internal/*` 返回 404 而不是落到前端兜底；请求体上限提到 24m，否则合法头像被 413 挡下
- Docker：`composer.lock` 变化时重新安装依赖（原先只判断 vendor 是否存在会静默跳过）
- 前端：切换菜单后内容区不刷新、新增页面路由注册不上、根路径重定向不生效、
  角色成员抽屉打不开、侧边栏二级菜单没画图标、深色模式首屏闪白、logo 区与顶栏底边没接上
- `v-permission` 拒绝挂到 Fragment 根组件，下拉项的权限收敛改用 `v-if`
- `scripts/acceptance.sh` 的仓库根写死成了作者本机的绝对路径，别人 clone 下来跑不了；
  改为从脚本自身位置推导，数据库账号密码也支持用 `DB_USERNAME` / `DB_PASSWORD` / `DB_DATABASE` 覆盖

### 安全
- 客户端 IP 可伪造，波及白名单 / 限流 / 审计
- 会话吊销与登录锁定两处缺陷
- 登录日志缺 `dept_id`，导致数据权限在非「仅本人」范围下找不到部门列而直接放行，
  部门主管能看到全公司记录
- 数据权限补上写入侧：可写部门集合 = 可读部门集合
- 脱敏缺陷：`Arr::mask('')` 把空手机号打成 `*`；操作日志的 `params` 存明文手机号绕过了字段级权限，
  已加部分脱敏档 `PARTIAL`

---

## 版本记录格式说明

每个版本按以下分类记录，无内容的分类可省略：

- **新增** — 新功能
- **变更** — 已有功能的调整
- **废弃** — 即将移除的功能（需给出替代方案与移除版本）
- **移除** — 已删除的功能
- **修复** — Bug 修复
- **安全** — 安全相关修复（关联安全公告编号）

破坏性变更需在条目前标注 `**BREAKING**` 并给出迁移步骤，例如：

```markdown
### 变更
- **BREAKING** 权限标识由 `sys:user:add` 改为 `sys:user:create`
  迁移：执行 `php start.php migrate:perm-rename`，或手工更新 `sys_permissions` 表中的标识
```

## 示例

<!--
## [1.1.0] - 2026-09-01

### 新增
- 角色管理支持字段级权限配置（#42）
- 列表页列设置支持按用户持久化（#38）

### 修复
- 修复角色回收后旧 token 仍可访问的问题（#51）
- 修复多进程下微信 access_token 互相顶掉的问题（#55）

### 安全
- 修复导出接口未走数据权限过滤导致的越权（GHSA-xxxx）
-->
