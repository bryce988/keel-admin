# 安全策略

Keel 是权限框架，安全问题的影响面通常比普通功能 Bug 大。请按本文档流程反馈。

## 支持的版本

| 版本 | 是否维护 |
|---|---|
| 最新的次版本（如 1.2.x） | ✅ 修复安全问题 |
| 上一个次版本 | ✅ 仅修复高危问题，持续 3 个月 |
| 更早版本 | ❌ 请升级 |

## 如何报告

**请不要开公开 Issue、不要在讨论区或社群贴出细节。**

首选 GitHub 的私密漏洞报告（无需邮箱，全程仅维护者可见）：

👉 https://github.com/bryce988/keel-admin/security/advisories/new

也可以在 Gitee 提一个**不含任何技术细节**的 Issue（标题写「安全问题反馈」），我们会主动联系你走私下渠道。

报告请包含：

- 影响的版本与组件（前端 / 后端 / 某个接口）
- 复现步骤，最好附最小复现代码或请求报文
- 你判断的影响范围（能读到什么数据、能执行什么操作）
- 是否已在公开渠道提及过

我们会在 **48 小时内确认收到**，7 天内给出评估结论与修复计划。修复发布后会在 CHANGELOG 与安全公告中致谢报告者（如你希望匿名请说明）。

## 我们特别关注的问题

这几类问题对本项目属于高危，欢迎重点测试：

- **越权**：横向越权（改 ID 读到别人的数据）、纵向越权（低权角色调用高权接口）
- **数据权限绕过**：通过导出、批量接口、关联查询绕开数据范围限制
- **字段级权限失效**：无权限账号从接口拿到了手机号明文、金额等敏感字段
- **权限缓存问题**：角色被回收后旧 token 仍能访问
- **多端身份混用**：C 端 token 调通了管理端接口，或反之
- **越权提权**：普通用户给自己授予角色、修改自己的数据范围
- 常规 Web 安全问题：SQL 注入、XSS、CSRF、文件上传绕过、SSRF

## 不在范围内

- 需要已获得服务器权限才能利用的问题
- 自动化扫描器的无验证输出
- 缺少某个安全响应头但无实际可利用路径
- 社会工程学、物理攻击
- 使用者自行修改框架代码后引入的问题

## 使用者自查建议

部署 Keel 时请确认：

- 生产环境未开启调试模式，异常不返回堆栈
- 默认管理员密码已修改，初始化脚本中的演示账号已清理
- JWT 密钥、数据库密码通过环境变量注入，未提交进仓库
- 管理后台接口未直接暴露公网，或已配置 IP 白名单
- 开启了操作日志与登录日志，并定期在「角色管理 → 分配权限」里核对每个角色的授权范围
- **`TRUSTED_PROXIES` 与你的实际拓扑一致**（见下）

### 客户端 IP 与反向代理

IP 白名单、限流、登录日志、操作日志四样东西全都依赖「客户端真实 IP」，
而这个值在有反向代理时只能从转发头里取——**取错就是四样一起失真**。

Keel 的规则（`app/common/support/ClientIp.php`）：

> 只有当 TCP 对端本身在 `TRUSTED_PROXIES` 里时，才采信它转发的 `X-Real-IP`；
> 否则一律用对端地址。**任何情况下都不读 `X-Forwarded-For`**——
> 它是链式追加的，最左段由客户端控制。

部署时要做的：

1. **代理必须覆盖式设置 `X-Real-IP`**，不能是追加或条件设置。
   仓库自带的 `docker/nginx/default.conf` 已经是 `proxy_set_header X-Real-IP $remote_addr;`。
2. **`TRUSTED_PROXIES` 收窄到你的网关地址**。默认值是三段私有网段，
   意味着同一内网里的任何主机都能声明客户端 IP——单机 docker 部署够用，
   多租户内网里不够。
3. **前面若还有 CDN / SLB**，nginx 那层要改成从上游头取真实 IP
   （如 `proxy_set_header X-Real-IP $http_x_real_ip;`），并把回源网段加进
   `TRUSTED_PROXIES`；否则记录到的是回源节点地址。
4. **不要把应用端口直接暴露公网**。绕过代理直连时对端就是客户端自己，
   IP 是准的，但代理层加的其他防护（限流、白名单前置）也一并绕过了。
   自带的 `docker-compose.prod.yml` 只发布 nginx 的端口。

自查方法：

```bash
curl -H 'X-Forwarded-For: 1.2.3.4' -H 'X-Real-IP: 9.9.9.9' https://你的域名/open/ping
```

返回的 `your_ip` 应当是你的真实出口 IP。如果回显成 `1.2.3.4` 或 `9.9.9.9`，
说明代理层配置有误，上面四样防护都处于可绕过状态。
