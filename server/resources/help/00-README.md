<!--
小k 的帮助文档（docs/ai-tech.md §6）

这个目录下的全部 .md 按文件名顺序拼进小k 的系统提示词，小k 回答「系统怎么用」只依据这里。
写的时候注意：
- 只写系统真实的行为。写错了小k 会照着错说，而且说得很肯定
- 每篇开头一行「# 标题」，小k 引用时会写「来源：帮助 · 标题」
- 提到页面时写菜单路径（系统管理 / 用户管理）并附路由（/system/user），小k 会按提问人的权限决定给不给跳转
- 全部加起来控制在 3 万字以内：超过了要改成检索（ai-tech §6），否则每次提问都要为它付钱
- 改完要 `php start.php reload`：提示词在进程里缓存着
本文件自身也会被拼进去，所以这段写在 HTML 注释里——模型看得到，但不会当成帮助内容引用
-->
