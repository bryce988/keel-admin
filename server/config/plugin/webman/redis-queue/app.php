<?php

declare(strict_types=1);

// 队列插件开关
//
// ⚠️ 这个文件看着没用，少了它整个插件不加载：webman 扫描各插件目录下的
// app.php，读 enable 决定要不要加载该插件的 process / redis 等配置。
// 少了它的表现是「消费进程压根不出现在启动列表里」，而且不报任何错——
// 队列投递照样成功，消息就是没人消费。
//
// 注：这里用行注释而不是块注释，是因为要提到带通配符的路径，
// 而路径里的 `*` 加 `/` 会把块注释提前闭合，后面整段被当成代码解析。

return [
    'enable' => true,
];
