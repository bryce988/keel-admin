/**
 * 业务码（与后端 `server/app/common/constant/BizCode.php` 逐条对应）
 *
 * 这是一份镜像，不是权威来源。 权威在 PHP 那边——这里存在的唯一理由是
 * 前端也要判码，而判码这件事以前是散在各页面里的裸数字（`{ 20101: 'username' }` 这种）。
 * 裸数字的问题不是难看：后端改一个码，PHP 侧有编译器和 grep 兜着，
 * 前端这边不报错、不告警，只是「冲突时红框不再标到那个输入框上」——
 * 没有任何测试能发现，只有用户点到才知道。
 *
 * 约定：改 `BizCode.php` 必须同步改本文件，`scripts/check-bizcode.sh` 会把两边
 * 的名字与值对一遍，对不上就退出非 0。全量镜像（而不是只镜像用得到的那十来个）
 * 是为了让那个校验能做严格集合相等——「哪些该镜像」一旦需要判断，
 * 这条约定就会慢慢烂掉。大部分常量前端用不到，正常。
 *
 * 用法：只在需要特殊交互时判码（把错误红框标到某个输入框上，见 `<FormDrawer>`
 * 的 `errorFields`）。普通提示看 HTTP 状态码就够了，
 * 文案一律用后端返回的 `message`，前端不维护第二份。
 */
export const BizCode = {
  // ---------------------------------------------------------------- 通用（10000-19999）
  UNAUTHORIZED: 10101,         // 登录已过期（token 缺失/无效/过期）
  TOKEN_TYPE_MISMATCH: 10102,  // 员工 token 调 C 端，或反之
  PASSWORD_CHANGED: 10103,     // 密码已变更，全部令牌失效
  FORBIDDEN: 10301,            // 缺少功能权限点
  DATA_SCOPE_DENIED: 10302,    // 数据权限不足（只在写路径抛：dept_id 超出可写范围；读路径走 404 伪装）
  FIELD_SCOPE_DENIED: 10303,   // 字段权限不足（预留，暂无抛出点）
  GENERAL_BAD_REQUEST: 10400,  // 通用业务错误（400 兜底，BusinessException 默认值）
  NOT_FOUND: 10404,            // 数据不存在或已被删除（含无权见的伪装）
  CONFLICT: 10409,             // 乐观锁冲突 / 唯一性默认值
  VALIDATION_FAILED: 10422,    // 参数校验失败（details 含字段级错误）
  RATE_LIMITED: 10429,         // 操作过于频繁（响应头带 Retry-After）
  INTERNAL_ERROR: 10500,       // 服务暂时不可用（仅返回 trace_id）

  // ---------------------------------------------------------------- 管理后台 · 认证（20000-20007）
  ACCOUNT_OR_PASSWORD_ERROR: 20001,  // 不区分账号不存在与密码错误
  ACCOUNT_DISABLED: 20002,           // 账号已被停用
  ACCOUNT_LOCKED: 20003,             // 账号已锁定，请 N 分钟后重试
  CAPTCHA_ERROR: 20004,              // 验证码错误/过期（⚠️ 当前实现实际走 VALIDATION_FAILED，待定）
  OLD_PASSWORD_ERROR: 20005,         // 原密码错误（改密码）
  PASSWORD_POLICY_VIOLATION: 20006,  // 新密码不符合安全策略
  PASSWORD_EXPIRED: 20007,           // ⚠️ 预留：密码过期功能未实现，后端不会返回这个码
  MAIL_NOT_CONFIGURED: 20008,        // 未配置 SMTP，邮箱登录不可用
  MAIL_SEND_FAILED: 20009,           // 邮件发送失败（真实原因只进后端日志）
  EMAIL_AMBIGUOUS: 20010,            // 该邮箱绑定了多个账号，无法据此定位身份

  // ---------------------------------------------------------------- 管理后台 · 用户（201xx）
  ACCOUNT_EXISTS: 20101,          // 账号已存在
  SUPER_ADMIN_PROTECTED: 20103,   // 不允许操作超级管理员
  DATA_HANDOVER_REQUIRED: 20104,  // 停用前先完成数据交接
  CANNOT_OPERATE_SELF: 20105,     // 不能删除/停用自己的账号
  PHONE_TAKEN: 20106,             // 手机号已被其他账号使用
  EMAIL_TAKEN: 20107,             // 邮箱已被其他账号使用

  // ---------------------------------------------------------------- 管理后台 · 部门（202xx）
  DEPT_CYCLE: 20202,         // 上级部门不能是自己或其子部门
  DEPT_HAS_CHILDREN: 20203,  // 部门下存在用户或子部门，无法删除

  // ---------------------------------------------------------------- 管理后台 · 角色（203xx）
  BUILTIN_ROLE_PROTECTED: 20302,    // 内置角色不允许修改或删除
  ROLE_HAS_USERS: 20303,            // 角色下存在用户，无法删除
  ROLE_MUTUAL_EXCLUSION: 20304,     // 角色互斥，不可同时授予
  ROLE_LIMIT_EXCEEDED: 20305,       // 超出单账号角色数量上限
  ROLE_INHERIT_CYCLE: 20306,        // 继承角色不可形成环
  DATA_SCOPE_REQUIRES_DEPT: 20307,  // 自定义数据范围至少要选择一个部门
  ROLE_INHERITED: 20308,            // 角色被其他角色继承，无法删除（≠ 20303 角色下有用户）

  // ---------------------------------------------------------------- 管理后台 · 菜单/权限（204xx）
  PERM_CODE_EXISTS: 20401,   // 权限标识已存在
  PERM_IN_USE: 20402,        // 权限点被角色引用，请改为停用
  MENU_CYCLE: 20403,         // 上级菜单不能是自己或其子节点
  MENU_HAS_CHILDREN: 20404,  // 节点下还有子节点，请先删除子节点（≠ 20402 被角色引用）

  // ---------------------------------------------------------------- 管理后台 · 字典（205xx）
  DICT_CODE_EXISTS: 20501,  // 字典编码已存在（同字典内字典项的值重复也用这个）
  DICT_ITEM_IN_USE: 20502,  // 字典项已被引用，不可修改其值

  // ---------------------------------------------------------------- 管理后台 · 参数（206xx）
  BUILTIN_PARAM_PROTECTED: 20601,  // 内置参数不可删除
  PARAM_KEY_EXISTS: 20602,         // 参数键已存在

  // ---------------------------------------------------------------- 管理后台 · 导入导出（207xx）
  EXPORT_LIMIT_EXCEEDED: 20701,  // 导出数据量超过上限
  EXPORT_FILE_GONE: 20702,       // 导出文件已过期或被回收（任务记录仍在）

  // ---------------------------------------------------------------- 管理后台 · 岗位（208xx）
  POST_HAS_USERS: 20802,    // 岗位下存在用户，无法删除

  // ---------------------------------------------------------------- C 端（30000-39999）
  CHANNEL_HEADER_MISSING: 30001,  // 缺少 X-Channel 请求头
  CHANNEL_UNSUPPORTED: 30002,     // 不支持的渠道标识

  // ---------------------------------------------------------------- C 端 · 账号（301xx）
  // 后台前端用不到这三个码，但这份表与后端是**严格集合相等**（scripts/check-bizcode.sh），
  // 少一个 CI 就红。C 端自己的客户端另有一份，不从这里引
  APP_ACCOUNT_OR_PASSWORD_ERROR: 30101,  // 手机号或密码错误（不区分账号不存在）
  APP_ACCOUNT_DISABLED: 30102,           // 账号已被封禁
  APP_LOGIN_LOCKED: 30103,               // 失败次数过多，临时锁定

  // ---------------------------------------------------------------- 开放平台（40000-49999）
  INVALID_SIGNATURE: 40101,  // 签名校验失败 / 缺少签名参数
  SIGNATURE_EXPIRED: 40102,  // 签名已过期
  DUPLICATE_NONCE: 40103,    // 请求已被处理，请勿重复提交
  UNKNOWN_APP_KEY: 40104,    // 未知的 app_key
  IP_NOT_ALLOWED: 40301,     // 来源 IP 不在白名单内
} as const

export type BizCodeValue = (typeof BizCode)[keyof typeof BizCode]
