---
version: 1.0
name: Keel Admin Design System
description: Keel Admin 当前界面规范。以高密度后台任务为核心，通过中性表面、发丝线、清晰对齐和克制的蓝色交互反馈建立统一、安静且可靠的视觉语言。
---

# Keel Admin 设计规范

本文件描述仓库当前已经落地的界面规则，是新增页面、复制模板和评审 UI 的共同依据。它覆盖应用外壳、页面面板、搜索、表格、表单、登录页、仪表盘、深色模式和响应式行为。

Keel Admin 借用了 Apple 视觉语言中克制、清晰和低装饰的部分，但服务对象是高密度管理后台。系统保留 14px 正文、紧凑表格、语义状态色和明确的数据层级，不照搬营销页面的大字号、超大留白和全屏内容块。

## 1. 设计目标

### 1.1 信息优先

- 页面首先帮助用户搜索、比较、编辑和追踪数据。
- 装饰不能抢过数据；层级主要靠表面色差、间距、字重和发丝线表达。
- 一屏内相同角色的内容使用相同尺寸、对齐和交互反馈。

### 1.2 安静而有层级

- 页面底色与内容面板只差一档明度。
- 固定在页面上的导航、卡片和面板不使用阴影。
- 下拉、气泡、弹窗等真正脱离文档流的浮层才使用轻阴影。
- 不使用装饰性渐变；数据图表可用渐变表达数据比例。

### 1.3 默认一致

- 通用组件的推荐样式直接作为默认值，新页面不靠额外 class 或布尔属性才能正确显示。
- 页面应从 `web/src/views/template/` 的对应页型开始，而不是从旧业务页随意复制。
- 例外必须显式声明，并能说明业务原因。

## 2. 实现来源与优先级

视觉实现分为两层：

1. `web/src/styles/index.css`：布局令牌、面板结构、通用组件行为、字号阶梯。
2. `web/src/styles/design.css`：最终颜色、圆角、阴影、菜单和视觉细节。

加载顺序为：

```text
Element Plus base
→ Element Plus dark variables
→ styles/index.css
→ styles/design.css
```

同名令牌以 `design.css` 的最终值为准。页面样式优先使用 `--keel-*` 和 `--el-*` 变量。品牌色和语义基色集中维护在 `design.css`；业务页面不要自行增加相近的蓝、灰、绿、橙或红。

覆盖 Element Plus 全局选择器时必须以 `:root` 提升特异性，例如：

```css
:root .el-table {
  --el-table-header-bg-color: var(--el-fill-color-light);
}
```

按需加载的 Element Plus 样式会晚于入口 CSS 注入；缺少 `:root` 的同权重规则可能被静默覆盖。`npm run check-css` 会检查这一约束。

## 3. 颜色

### 3.1 浅色主题

| 角色 | 值 | 用途 |
|---|---:|---|
| Action Blue | `#0066cc` | 主按钮、链接、选中态和主要交互 |
| Focus Blue | `#0071e3` | 键盘焦点环 |
| Ink | `#1d1d1f` | 标题和主要文字 |
| Regular Ink | `#333333` | 正文和常规控件文字 |
| Muted Ink | `#7a7a7a` | 次要说明和辅助信息 |
| Canvas | `#ffffff` | 面板、卡片、输入区域 |
| Parchment | `#f5f5f7` | 页面背景、表头和中性悬浮底色 |
| Hairline | `#e0e0e0` | 主要分隔线 |

语义色保留独立含义：

| 语义 | 浅色基色 | 用途 |
|---|---:|---|
| 成功 | `#248a3d` | 成功、启用、健康 |
| 警告 | `#b25000` | 注意、等待、积压 |
| 危险 | `#d70015` | 失败、停用、删除 |
| 信息 | `#6e6e73` | 中性状态和补充说明 |

同一动作只使用一套主色派生值。悬浮态与选中态必须能区分：中性灰表示“鼠标经过”，浅蓝表示“当前选中”。

### 3.2 深色主题

- 页面底色为纯黑，内容面板为近黑，浮层再提高一档明度。
- 主交互色切换为 `#2997ff`，保证在近黑背景上的可读性。
- 成功、警告、危险分别使用 `#30d158`、`#ff9f0a`、`#ff453a`。
- 深色层级主要依赖表面明度差，阴影只辅助浮层边界。
- 组件必须使用语义变量，不为深色模式另写业务颜色。

## 4. 字体与数字

字体使用系统字体栈，macOS 和 iOS 上自然解析为 SF Pro。正文统一开启抗锯齿。

| 层级 | 字号 | 字重 | 使用场景 |
|---|---:|---:|---|
| 正文 | 14px | 400 | 表格、表单、菜单、普通文字 |
| 正文强调 | 14px | 500 | 按钮、链接、表头、紧凑标题 |
| 页签 | 13px | 400/500 | 页签和部分次级标签 |
| 辅助文字 | 12px | 400 | 提示、说明、标签文字 |
| 区块标题 | 16px | 600 | 表格标题、详情区标题 |
| 页面重点 | 20px | 600 | 登录标题等独立重点 |
| 指标数字 | 24–40px | 600 | 仪表盘指标和状态数字 |

规则：

- 14px 及以下只使用 400 和 500 两档字重。
- 16px 及以上才使用 600。
- 正文默认字距为 `-0.224px`，标题为 `-0.374px`；12px 辅助文字不收紧字距。
- 表格、统计卡和指标中的数字添加 `.num`，启用等宽数字 `tabular-nums`。
- 不通过同时增加字号、字重、颜色和背景来重复强调同一信息。

## 5. 尺寸、间距与圆角

### 5.1 全局令牌

| 令牌 | 当前值 | 用途 |
|---|---:|---|
| `--keel-panel-pad` | 16px | 面板和卡片内边距 |
| `--keel-gap` | 12px | 面板内部区块间距 |
| `--keel-gap-lg` | 16px | 页面面板之间的间距 |
| `--keel-radius` | 8px | 按钮、输入框、标签、页签、图标按钮 |
| `--keel-radius-lg` | 14px | 面板、卡片、对话框和登录卡 |
| `--keel-radius-pill` | 9999px | 明确需要胶囊语法的少数控件 |
| `--keel-topbar-height` | 56px | 顶栏、侧栏品牌区、分栏模块标题 |
| `--keel-tags-height` | 40px | 页签栏 |
| `--keel-sidebar-width` | 210px | 展开侧栏 |
| `--keel-sidebar-collapsed` | 64px | 折叠侧栏 |
| `--keel-rail-width` | 80px | 分栏模式一级导航 |

### 5.2 间距规则

- 页面根容器使用 `.page`，面板之间保持 16px。
- 面板内部标题、工具栏、正文和分页之间使用 12px；精炼表格工具栏与表格之间使用 16px。
- 桌面内容区内边距为 16px，900px 以下为 12px。
- 同一层级的间距使用 `gap`，不要再叠加子项 `margin-bottom`。

### 5.3 圆角规则

- 可操作控件统一 8px。
- 承载内容的容器统一 14px。
- 后台工具按钮不使用胶囊圆角；全局胶囊目前只用于顶栏独立搜索框等足够高、足够宽的控件。
- 圆形仅用于头像和没有文字的明确圆形入口。

## 6. 表面、边框与阴影

标准面板使用全局 `.panel`：

```css
.panel {
  padding: var(--keel-panel-pad);
  background: var(--el-bg-color);
  border: 1px solid var(--el-border-color-lighter);
  border-radius: var(--keel-radius-lg);
}
```

- `el-card` 与 `.panel` 使用相同内边距、边框和容器圆角。
- 页面上的卡片和面板不使用投影。
- 侧栏、顶栏和页签使用发丝线建立边界。
- 分栏模式的一级导航条比二级侧栏深一档，帮助识别导航层级。
- 不在 `SearchForm` 或 `ProTable` 外再套一层 `.panel`；它们自身已经是面板。

## 7. 应用外壳与导航

系统支持三种桌面导航布局：

- **经典**：侧栏通高，顶栏位于内容区上方。
- **混合**：顶栏通宽并承载一级菜单，二级侧栏位于顶栏下方。
- **分栏**：80px 一级导航条 + 210px 二级侧栏 + 内容区。

顶栏、侧栏品牌区和分栏模块标题必须共用 56px 高度与同一档底边线，使横线连续。

内容区域是桌面端唯一的页面滚动容器；应用外壳固定在视口内。900px 以下切换为单列堆叠并恢复整页滚动。

### 7.1 侧栏菜单

- 菜单项基准高度为 44px。
- 展开态菜单项只允许左右外边距：`margin: 0 8px`。
- 二级菜单通过左侧 20px 缩进表达层级。
- 行间气口使用 2px 透明上下边框和 `background-clip: padding-box`，不能使用上下 margin。
- `.el-menu--inline` 不设置上下 padding 或 margin。
- 选中项使用最浅主色底和主色文字；悬浮项使用中性底色。

禁止为 `.el-menu-item` 或 `.el-sub-menu__title` 添加非零 `margin-top`、`margin-bottom`、`margin-block*`，也不能通过 `margin` 简写引入上下外边距。Element Plus 的折叠动画会先读取高度再创建 BFC，外边距折叠状态变化会导致首帧跳动。该规则由 `npm run check-css` 扫描整个 `src/layout/**/*.vue`。

## 8. 页面结构

### 8.1 标准列表页

```vue
<div class="page">
  <SearchForm v-model="query" :fields="searchFields" @search="reload" />
  <ProTable
    ref="tableRef"
    v-model:params="query"
    :request="fetchRows"
    :columns="columns"
    title="业务列表"
  />
</div>
```

- 搜索面板在上，表格面板在下，间距由 `.page` 统一提供。
- 表格必须提供清晰的 `title`。
- 新增、导入、导出等业务操作放在表格 `toolbar` 左侧。
- 刷新、密度、列设置放在右侧工具区。
- 默认可见列尽量不超过 8 列，次要信息通过 `hidden: true` 收入列设置。

### 8.2 现有页型模板

| 模板 | 适用场景 |
|---|---|
| `template/list` | 搜索 + 分页列表 + 抽屉增改查 |
| `template/tree-list` | 左侧分类树 + 右侧列表 |
| `template/master-detail` | 主记录列表 + 从表编辑 |
| `template/detail` | 独立详情页 + 内嵌从表 |
| `template/form` | 独立长表单 + 固定动作区 |

复制模板后保留其布局、对齐和高度语义，只替换字段与业务逻辑。

## 9. SearchForm

`SearchForm` 的紧凑布局是默认样式，不需要额外 class。

- 根节点自身使用 `.panel`。
- 字段区使用等宽栅格：`repeat(auto-fit, minmax(min(100%, 260px), 1fr))`。
- 标签宽度为 72px；新增标签尽量控制在 4 个汉字以内。
- 输入、下拉、日期控件和非链接按钮高度为 36px。
- 查询、重置和展开按钮独占右侧动作列，不随字段换行移动。
- 字段多于折叠阈值时默认只显示首行，通过“展开/收起”切换。
- 容器宽度低于 650px 时字段区与动作区上下排列，动作区右对齐。

字段宽度由栅格决定，不再按输入框、下拉框、日期框分别写固定宽度。

## 10. ProTable

### 10.1 默认模式

`ProTable` 当前默认值：

```ts
refined = true
height = 'fit'
pageSize = 20
syncUrl = true
selection = false
immediate = true
```

精炼模式使用无竖向网格、无斑马纹的轻量列表。表头通过字重、浅表面色和底边线建立层级，行悬浮使用最浅蓝色。传统网格仅用于明确需要完整单元格边框的场景，并显式传入 `:refined="false"`。

### 10.2 高度语义

| 值 | 行为 | 适用场景 |
|---|---|---|
| `fit` | 默认；少量数据随内容收缩，超出视口后表体滚动 | 普通列表页 |
| `fill` | 表体固定填满可用高度，分页位置稳定 | 需要固定工作区的列表 |
| `auto` | 不限制表格高度，由页面滚动 | 仪表盘下方、详情页、主从页等嵌入场景 |

不要再增加 `lockHeight`、`fitContent` 等控制同一行为的布尔属性。

### 10.3 列对齐

每个页面的 `ProColumn` 都要显式声明 `align`：

- **左对齐**：名称、编码、账号、时间、IP、路径、描述、备注和其他可阅读文本。
- **居中**：ID、数字、排序、状态、枚举标签、图标和操作列。
- 树形表第一列必须左对齐，确保展开箭头和层级缩进正确。
- refined 模式下表头跟随正文列对齐；传统模式表头保持居中。

```ts
const columns: ProColumn<UserRow>[] = [
  { prop: 'username', label: '账号', minWidth: 150, align: 'left' },
  { prop: 'created_at', label: '创建时间', minWidth: 190, align: 'left' },
  { prop: 'status', label: '状态', width: 88, align: 'center', slot: 'status' },
  { prop: 'actions', label: '操作', width: 200, align: 'center', fixed: 'right', slot: 'actions' }
]
```

### 10.4 列宽

- 文本列优先使用 `minWidth`，让宽屏剩余空间自然分配。
- 图标、状态、数字、开关和操作等定长列使用 `width`。
- 单元格左右内边距按 12–16px 计算；测算文本宽度时至少为一列额外预留 32px。
- 常用基准：时间 190px、完整 IPv4 150px、手机号 140px、5 字姓名 120px、Trace ID 170px、较长权限标识 210px。
- 排序表头额外预留约 24px；标签内容额外预留约 20px。
- 超长用户数据允许通过 tooltip 降级，但内置种子数据和常见值不能默认截断。

### 10.5 分页与工具

- 分页位于面板右下方，与表格保持 12px 间距。
- 总数不足一页时隐藏跳页输入，减少无效控件。
- 右侧工具按钮为 34px 方形、8px 圆角、中性浅底；悬浮后使用主色反馈。
- ID 固定在最左，操作列通常固定在最右；树形表不自动插入 ID 列。

## 11. 表单、抽屉与弹窗

- 新增、编辑和详情统一优先使用 `FormDrawer`。
- 默认抽屉宽度为 560px，标签宽度为 96px，表单项垂直间距为 18px。
- 抽屉不允许点击遮罩误关闭；关闭后销毁内容，避免旧状态残留。
- 只读详情隐藏确认按钮，只保留“关闭”。
- 底部操作右对齐，按钮间距为 12px。
- 改密码、换绑手机等字段少、动作单一的任务使用 `FormDialog`。
- 表单顶部说明使用 `el-alert`，与首个字段保持 18px 间距。

独立长表单使用 `template/form` 的分区面板和底部动作区，避免把大量字段塞进过窄弹窗。

## 12. 仪表盘与登录页

### 12.1 仪表盘

- 指标卡使用自适应栅格，单卡最小宽度 220px。
- 指标数字使用 30px、600 字重和等宽数字。
- 多张卡在同一行时底部说明对齐；内容少的卡通过弹性空间将卡脚推到底部。
- 可点击卡片只在悬浮时改变边框色，不增加漂浮阴影或位移动画。
- 图表颜色使用语义变量，并提供文字、数值或 tooltip，不能只靠颜色传达状态。

### 12.2 登录页

- 登录区域使用居中的双栏卡片：左侧品牌，右侧表单。
- 卡片使用白色表面、发丝线和 14px 圆角，不使用阴影或装饰性渐变。
- 品牌装饰使用低透明度主色图形，不参与布局，避免表单校验时发生位移。
- 登录按钮通栏、高 42px；验证码图片与相邻输入控件共用 8px 圆角。
- 860px 以下切为单列，品牌区收成横向标识栏，隐藏非必要装饰。

## 13. 动效与可访问性

- 普通按钮按下缩放为 `0.96`，只提供短促反馈，不移动页面布局。
- 悬浮颜色过渡通常为 150–200ms；导航栏宽度切换为约 280ms。
- `prefers-reduced-motion: reduce` 下取消位移或缩放反馈。
- 所有图标按钮必须提供 `aria-label` 或 tooltip。
- 可点击元素使用原生 `button`、链接或 Element Plus 交互组件，不能只给 `div` 绑定点击事件。
- 键盘焦点必须可见，使用 2px Focus Blue 轮廓。
- 状态不能只依靠颜色区分，需同时提供文字、图标或形状。
- 文本截断必须有 tooltip 或 `title` 提供完整内容。

## 14. 响应式规则

| 断点 | 行为 |
|---:|---|
| 900px | 应用外壳切为单列、整页滚动，内容内边距降为 12px |
| 860px | 登录卡由双栏切为单栏 |
| 650px 容器宽度 | 搜索字段与动作区上下排列 |
| 600px | ProTable 标题独占一行，分页改为左对齐并允许横向滚动 |

响应式优先保证任务可完成和内容可读。不要通过缩小正文字号塞入更多内容；应先换行、堆叠、横向滚动或隐藏次要列。

## 15. 新页面检查清单

- [ ] 从最接近的 `views/template/` 页型复制。
- [ ] 页面根节点使用 `.page`，标准内容容器使用 `.panel` 或 `el-card`。
- [ ] 没有在 `SearchForm`、`ProTable` 外重复套面板。
- [ ] 表格填写标题，文本列显式左对齐，状态、数字和操作列显式居中。
- [ ] 文本列优先使用 `minWidth`，常见数据无需默认查看 tooltip。
- [ ] 普通列表保持 `height="fit"` 默认；嵌入表格显式使用 `height="auto"`。
- [ ] 默认可见列控制在约 8 列，次要列收入列设置。
- [ ] 颜色、间距、圆角和阴影来自现有令牌。
- [ ] 图标按钮有可访问名称，键盘焦点可见。
- [ ] 浅色、深色、900px 以下和空数据状态都可正常使用。
- [ ] 没有给侧栏菜单项添加上下外边距。
- [ ] 完成后运行 `npm run check`。

## 16. 禁止事项

- 不在业务页面新增硬编码品牌色或相近灰阶。
- 不用重阴影抬高普通卡片和面板。
- 不使用装饰性渐变、玻璃拟态或无意义的大面积插画。
- 不把后台工具按钮全部做成胶囊。
- 不用多个布尔属性共同控制一个组件维度。
- 不依赖 `ProTable` 的隐式列对齐；页面必须显式写 `align`。
- 不为不同列表页各写一套搜索栏、工具栏和分页布局。
- 不在 scoped 样式中以同权重规则覆盖后加载的 Element Plus 全局样式。
- 不通过菜单项上下 margin 或内联菜单上下 padding 制造行间距。
